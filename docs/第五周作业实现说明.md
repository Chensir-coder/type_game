# 第五周作业实现说明

## 一、本周开发目标

第五周的核心目标是：  
在“拯救苹果”游戏基础上，完成以下功能模块的工程化实现：

- 音频播放系统（背景音乐 + 音效）
- 日志系统（运行日志记录）
- 埋点系统（行为数据统计）
- 基于多线程的异步处理机制

本周重点不在游戏玩法，而在于**系统能力建设与架构扩展能力**。

---

## 二、整体设计思路

在不破坏原有 MVC 架构的前提下，本周采用**事件驱动 + 异步服务化设计**：

- 游戏核心逻辑（Model）**仅负责产生事件（GameEvent）**
- 各功能模块（日志 / 音频 / 埋点）通过**观察者模式订阅事件**
- 所有耗时操作（IO / 音频播放）通过**独立线程异步执行**

整体处理结构如下：

```
Game(Model)
↓ 发布事件
Observer（日志 / 音频 / 埋点）
↓
Service（异步处理线程）

```
事件类型定义如下：
```

// 事件类型定义
enum class GameEventType
{
    Unknown = 0,       ///< 未知事件

    GameInitialized,   ///< 游戏初始化完成
    GameStarted,       ///< 游戏开始
    GamePaused,        ///< 游戏暂停
    GameResumed,       ///< 游戏恢复
    GameStopped,       ///< 游戏停止
    GameRestarted,     ///< 游戏重开
    GameStateChanged,  ///< 游戏状态变化

    ScoreChanged,      ///< 分数变化
    LevelChanged,      ///< 等级变化
    LevelCompleted,    ///< 关卡完成
    HpChanged,         ///< 生命值变化

    EntitySpawned,     ///< 实体生成
    EntityRemoved,     ///< 实体移除
    EntityHit,         ///< 实体被命中
    EntityMissed,      ///< 实体漏掉/失败

    InputAccepted,     ///< 输入被接受
    InputRejected,      ///< 输入被拒绝

    SettingsChanged,   ///< 设置变化    
    GameOver,           ///< 游戏结束

};
```
主游戏模型在游戏运行过程中，在对应状态发送相应事件通知其观察者列表中的观察者。

具体服务模块（音频、日志、埋点）统一添加到主游戏模型的观察者列表，并观察上述事件的派发，挂载方式核心代码如下：
```
 // 1. 创建核心组件
m_model = std::make_unique<SaveAppleModel>();
m_apple_factory = std::make_unique<AppleFactory>();
m_controller = std::make_unique<SaveAppleController>(
    m_model.get(),
    m_apple_factory.get());

// 2. 创建日志组件
m_log_formatter = std::make_unique<PlainTextLogFormatter>();
const QString log_file_path = buildLogFilePath();
m_logger = std::make_unique<AsyncLogger>(
    log_file_path,
    m_log_formatter.get()
);
m_logger->start();
m_logger_observer = std::make_unique<SaveAppleLoggerObserver>(m_logger.get());

// 3. 创建 Telemetry 组件
m_telemetry_writer = std::make_unique<AsyncTelemetryWriter>(buildTelemetryFilePath());
m_telemetry_writer->start();

m_telemetry_observer = std::make_unique<SaveAppleTelemetryObserver>(m_telemetry_writer.get());

// 4. 创建音频组件
m_audio_service = std::make_unique<QtAudioService>();
m_audio_service->initialize();
m_audio_service->setEffectVolume(0.8f);
m_audio_service->setBackgroundMusicVolume(0.45f);


m_audio_observer = std::make_unique<SaveAppleAudioObserver>(m_audio_service.get());
m_audio_observer->registerDefaultCues();
m_audio_observer->registerBackgroundMusic();
m_audio_observer->syncSoundEnabled(m_model->soundEnabled());

// 4. 连接模型与观察者
m_model->addObserver(m_telemetry_observer.get());
m_model->addObserver(m_logger_observer.get());
m_model->addObserver(m_audio_observer.get());

```

该设计实现了核心逻辑与扩展功能的解耦。

---

## 三、核心模块实现

### 3.1 音频播放模块

#### 设计目标
- 支持背景音乐循环播放
- 支持击中、失败等音效播放
- 避免音频播放阻塞主线程

#### 实现方案
- 使用 Qt 提供的音频组件：
  - `QMediaPlayer`：背景音乐
  - `QSoundEffect`：短音效
- 构建 `QtAudioService` 音频服务类
- 引入 `QtAudioWorker` 工作线程处理音效播放
- 使用命令队列（AudioCommand）进行任务分发

#### 音频服务关键接口设计如下
```
class AudioService
{
public:
    virtual ~AudioService() = default;

    virtual void initialize() = 0;
    virtual void shutdown() = 0;

    // ---------- 短音效 ----------
    virtual void registerCue(AudioCue cue, const QString& source_path) = 0;
    virtual void playCue(AudioCue cue) = 0;
    virtual void stopCue(AudioCue cue) = 0;
    virtual void stopAllCues() = 0;

    // ---------- 背景音乐 ----------
    virtual void setBackgroundMusic(const QString& source_path) = 0;
    virtual void playBackgroundMusic() = 0;
    virtual void pauseBackgroundMusic() = 0;
    virtual void resumeBackgroundMusic() = 0;
    virtual void stopBackgroundMusic() = 0;
    virtual bool isBackgroundMusicPlaying() const = 0;

    // ---------- 总开关 ----------
    virtual void setEnabled(bool enabled) = 0;
    virtual bool isEnabled() const = 0;

    // ---------- 音量 ----------
    virtual void setEffectVolume(float volume) = 0;
    virtual float effectVolume() const = 0;

    virtual void setBackgroundMusicVolume(float volume) = 0;
    virtual float backgroundMusicVolume() const = 0;
};
```

#### 特点
- 音频播放与游戏逻辑完全解耦
- 支持异步播放多个音效
- 支持开关控制（通过事件驱动）

---

### 3.2 日志系统

#### 设计目标
- 记录游戏运行过程中的关键事件
- 不影响游戏主线程性能

#### 实现方案
- 封装 `AsyncLogger` 类
- 使用：
  - `std::thread`
  - `QMutex`
  - `QWaitCondition`
  - 队列缓存日志消息
- 后台线程负责写入文件

### 日志服务关键接口设计如下：
```
class ILogger
{
public:
    virtual ~ILogger() = default;

    virtual void start() = 0;
    virtual void stop() = 0;
    virtual void log(const LogMessage& message) = 0;
};

class AsyncLogger : public ILogger
{
public:
    AsyncLogger(const QString& file_path, ILogFormatter* formatter);
    ~AsyncLogger();

    void start() override;
    void stop() override;
    void log(const LogMessage& message) override;

private:
    void worker_loop();

private:
    QString m_file_path;
    ILogFormatter* m_formatter = nullptr;

    bool m_running = false;
    std::thread m_thread;

    QFile m_file;
    QMutex m_mutex;
    QWaitCondition m_condition;
    QQueue<LogMessage> m_queue;
};

```

#### 日志内容示例

```
[2026-04-19 20:30:50.644][INFO][saveapple] Game initialized
[2026-04-19 20:30:50.693][INFO][saveapple] Settings changed: key=FailLineY value=503
[2026-04-19 20:31:18.420][INFO][saveapple] Settings changed: key=MaxAppleCount value=3
[2026-04-19 20:31:18.420][INFO][saveapple] Settings changed: key=TargetSuccessCount value=5
[2026-04-19 20:31:18.420][INFO][saveapple] Settings changed: key=MaxMissCount value=3
[2026-04-19 20:31:18.420][INFO][saveapple] Settings changed: key=SoundEnabled value=0
[2026-04-19 20:31:20.378][INFO][saveapple] Game started
[2026-04-19 20:31:21.290][DEBUG][saveapple] Apple spawned: letter=X
[2026-04-19 20:31:22.202][DEBUG][saveapple] Apple spawned: letter=I
[2026-04-19 20:31:23.114][DEBUG][saveapple] Apple spawned: letter=K
[2026-04-19 20:31:26.426][INFO][saveapple] Apple hit: letter=X score=10
[2026-04-19 20:31:26.763][DEBUG][saveapple] Apple spawned: letter=Y
[2026-04-19 20:31:30.088][INFO][saveapple] Apple hit: letter=I score=20
[2026-04-19 20:31:30.412][DEBUG][saveapple] Apple spawned: letter=G
[2026-04-19 20:31:31.786][WARN][saveapple] Apple missed: letter=K miss=1
[2026-04-19 20:31:32.235][DEBUG][saveapple] Apple spawned: letter=F
[2026-04-19 20:31:33.738][INFO][saveapple] Apple hit: letter=Y score=25
[2026-04-19 20:31:34.060][DEBUG][saveapple] Apple spawned: letter=U
[2026-04-19 20:31:39.083][WARN][saveapple] Apple missed: letter=G miss=2
[2026-04-19 20:31:39.531][DEBUG][saveapple] Apple spawned: letter=T
[2026-04-19 20:31:40.171][INFO][saveapple] Apple hit: letter=F score=30
[2026-04-19 20:31:40.443][DEBUG][saveapple] Apple spawned: letter=Q
[2026-04-19 20:31:41.575][INFO][saveapple] Apple hit: letter=U score=40
[2026-04-19 20:31:41.578][INFO][saveapple] Level completed: score=40 success=5
[2026-04-19 20:31:41.578][WARN][saveapple] Game over: score=40 miss=2
[2026-04-19 20:31:46.475][INFO][saveapple] Settings changed: key=SpeedLevel value=2
[2026-04-19 20:31:46.475][INFO][saveapple] Game started
[2026-04-19 20:31:47.388][DEBUG][saveapple] Apple spawned: letter=G
[2026-04-19 20:31:48.299][DEBUG][saveapple] Apple spawned: letter=B
[2026-04-19 20:31:49.211][DEBUG][saveapple] Apple spawned: letter=Q
[2026-04-19 20:31:49.870][INFO][saveapple] Game paused
[2026-04-19 20:31:51.004][INFO][saveapple] Game resumed
[2026-04-19 20:31:51.564][INFO][saveapple] Game stopped

```

#### 特点
- 异步写入，避免 IO 阻塞
- 支持日志格式化（Formatter）
- 可扩展不同日志输出方式

---

### 3.3 埋点系统

#### 设计目标
- 统计玩家行为数据
- 为后续数据分析或优化提供支持

#### 实现方案
- 封装 `AsyncTelemetryWriter`
- 与日志系统类似，采用：
  - 队列缓存
  - 后台线程写入
- 数据格式为 JSON（便于分析）

#### 埋点服务关键接口如下：
```
class ITelemetryWriter
{
public:
    virtual ~ITelemetryWriter() = default;

    virtual void start() = 0;
    virtual void stop() = 0;
    virtual void write(const TelemetryMessage& message) = 0;
};

class AsyncTelemetryWriter : public ITelemetryWriter
{
public:
    AsyncTelemetryWriter(const QString& file_path);
    ~AsyncTelemetryWriter();

    void start() override;
    void stop() override;
    void write(const TelemetryMessage& message) override;

private:
    void worker_loop();

private:
    QString m_file_path;
    JsonFormatter m_formatter;

    bool m_running = false;
    std::thread m_thread;

    QFile m_file;
    QMutex m_mutex;
    QWaitCondition m_condition;
    QQueue<TelemetryMessage> m_queue;
};

```
#### 埋点内容示例
```
{"category":"saveapple","event_name":"game_initialized","payload":{"level":1,"score":0,"state":0},"timestamp_ms":1776601850644}
{"category":"saveapple","event_name":"game_state_changed","payload":{"level":1,"score":0,"state":0},"timestamp_ms":1776601850644}
{"category":"saveapple","event_name":"score_changed","payload":{"level":1,"score":0,"state":0},"timestamp_ms":1776601850644}
{"category":"saveapple","event_name":"setting_changed_fail_line_y","payload":{"level":1,"score":0,"state":0,"value":503},"timestamp_ms":1776601850693}
{"category":"saveapple","event_name":"setting_changed_max_apple_count","payload":{"level":1,"score":0,"state":0,"value":3},"timestamp_ms":1776601878420}
{"category":"saveapple","event_name":"setting_changed_target_success_count","payload":{"level":1,"score":0,"state":0,"value":5},"timestamp_ms":1776601878420}
{"category":"saveapple","event_name":"setting_changed_max_miss_count","payload":{"level":1,"score":0,"state":0,"value":3},"timestamp_ms":1776601878420}
{"category":"saveapple","event_name":"setting_changed_sound_enabled","payload":{"level":1,"score":0,"state":0,"value":false},"timestamp_ms":1776601878420}
{"category":"saveapple","event_name":"game_started","payload":{"level":1,"score":0,"state":1},"timestamp_ms":1776601880378}
{"category":"saveapple","event_name":"game_state_changed","payload":{"level":1,"score":0,"state":1},"timestamp_ms":1776601880378}
{"category":"saveapple","event_name":"apple_spawned","payload":{"active_apple_count":1,"letter":"X","level":1,"score":0,"state":1},"timestamp_ms":1776601881290}
{"category":"saveapple","event_name":"apple_spawned","payload":{"active_apple_count":2,"letter":"I","level":1,"score":0,"state":1},"timestamp_ms":1776601882202}
{"category":"saveapple","event_name":"apple_spawned","payload":{"active_apple_count":3,"letter":"K","level":1,"score":0,"state":1},"timestamp_ms":1776601883114}
{"category":"saveapple","event_name":"apple_hit","payload":{"letter":"X","level":1,"miss_count":0,"score":10,"state":1,"success_count":1},"timestamp_ms":1776601886425}
{"category":"saveapple","event_name":"apple_removed","payload":{"active_apple_count":2,"letter":"X","level":1,"score":10,"state":1},"timestamp_ms":1776601886426}
{"category":"saveapple","event_name":"score_changed","payload":{"level":1,"score":10,"state":1},"timestamp_ms":1776601886426}
{"category":"saveapple","event_name":"apple_spawned","payload":{"active_apple_count":3,"letter":"Y","level":1,"score":10,"state":1},"timestamp_ms":1776601886763}
{"category":"saveapple","event_name":"apple_hit","payload":{"letter":"I","level":1,"miss_count":0,"score":20,"state":1,"success_count":2},"timestamp_ms":1776601890088}
{"category":"saveapple","event_name":"apple_removed","payload":{"active_apple_count":2,"letter":"I","level":1,"score":20,"state":1},"timestamp_ms":1776601890088}

```

#### 特点
- 与日志系统解耦
- 可扩展为上传服务器（未来扩展点）
- 支持失败降级（不影响游戏）

---

### 3.4 观察者模式（核心机制）

#### 设计目的
避免在游戏核心逻辑中直接调用日志/音频/埋点代码。

#### 实现方式
- 定义统一事件结构 `GameEvent`
- Model 负责发布事件
- 各 Observer 订阅并处理：
  - `SaveAppleLoggerObserver`
  - `SaveAppleAudioObserver`
  - `SaveAppleTelemetryObserver`

#### 优势
- 符合开闭原则（新增功能无需修改核心逻辑）
- 模块之间低耦合
- 易于扩展新功能

---

### 3.5 多线程设计

本周重点之一是多线程应用：

| 模块       | 是否多线程 | 作用 |
|------------|------------|------|
| 音频系统   | 是         | 异步播放音效 |
| 日志系统   | 是         | 异步写文件 |
| 埋点系统   | 是         | 异步写数据 |

#### 实现要点
- 使用线程 + 队列模型
- 使用互斥锁保证线程安全
- 使用条件变量避免空轮询

#### 优点
- 主线程只负责游戏逻辑和渲染
- 提升整体响应速度
- 提高系统稳定性

---

## 四、与架构设计的结合

本周实现严格遵循课程中讲解的设计原则：

### 4.1 MVC 分层

- Model：游戏数据与逻辑
- View：UI 展示
- Controller：控制流程
- Service：日志 / 音频 / 埋点（新增）

### 4.2 设计模式应用

| 模式       | 使用位置 |
|------------|----------|
| 观察者模式 | 事件通知机制 |
| 工厂模式   | AppleFactory |
| 命令模式   | AudioCommand |
| 单例模式   | （未强制使用，避免滥用） |

---


## 七、小结

本周在原有游戏功能基础上，完成了**从“功能实现”向“工程化能力”的提升**：

- 引入多线程机制
- 构建可扩展的服务模块（音频 / 日志 / 埋点）
- 使用观察者模式实现解耦
- 为后续“太空大战”复用打下基础

整体实现达到了课程对第五周的核心要求，并具备良好的扩展性。
